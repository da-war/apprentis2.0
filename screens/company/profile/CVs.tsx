import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CVs = () => {
  return (
    <View>
      <Text>CVs</Text>
    </View>
  );
};

export default CVs;

const styles = StyleSheet.create({});
